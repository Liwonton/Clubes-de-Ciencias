import java.util.Collections;
import java.util.HashSet;
import java.util.Set;

void keyPressed(){
  if(key == ' ') println("HI!");
  if(key == 'e') init();
  if(key == 'w') background(c, 255, 255);
}
